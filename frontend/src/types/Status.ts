const STATUS = ["Online", "Offline", "Idle", "Do Not Disturb"];
const STATUS_ID = ["online", "offline", "idle", "dnd"];

enum StatusEnum {
    ONLINE,
    OFFLINE,
    IDLE,
    DND,
}

export { STATUS, STATUS_ID, StatusEnum };
